---
title: "Terraform Series #2: Installation and AWS CLI Configuration"
date: 2026-01-24 16:00:00 +1100
categories: [DevOps, Terraform]
tags: [terraform, aws, linux, windows, macos, guide]
description: A complete guide to installing Terraform and AWS CLI on Linux, Windows, and macOS, including credential setup.
---

In the first post of this series, we explored why Infrastructure as Code (IaC) is a game-changer for modern infrastructure.  
Now it is time to set up your workstation so you can begin building.

In this post, we will cover the installation of **Terraform** and the **AWS CLI** on all major operating systems and show you how to link them to your cloud account.

---

## 1. Installing Terraform

Terraform is distributed as a single binary. While you can download it manually, using a package manager is the best way to handle future updates and dependencies.

### Linux (Ubuntu / 
sudo apt-get update && sudo apt-get install -y gnupg software-properties-common

wget -O- https://apt.releases.hashicorp.com/gpg \
| gpg --dearmor \
| sudo tee /usr/share/keyrings/hashicorp-archive-keyring.gpg

echo "deb [signed-by=/usr/share/keyrings/hashicorp-archive-keyring.gpg] \
https://apt.releases.hashicorp.com $(lsb_release -cs) main" \
| sudo tee /etc/apt/sources.list.d/hashicorp.list

sudo apt-get update && sudo apt-get install terraform

Linux (CentOS / RHEL)
sudo yum install -y yum-utils
sudo yum-config-manager --add-repo https://rpm.releases.hashicorp.com/RHEL/hashicorp.repo
sudo yum -y install terraform

macOS
brew tap hashicorp/tap
brew install hashicorp/tap/terraform

Windows
choco install terraform

Manual method:

Download Terraform ZIP
Extract to C:\terraform
Add folder to PATH

Verify Installation
terraform --version

2. Installing the AWS CLI

Terraform communicates with AWS using the AWS Command Line Interface.

Linux
curl "https://awscli.amazonaws.com/awscli-exe-linux-x86_64.zip" -o "awscliv2.zip"
unzip awscliv2.zip
sudo ./aws/install

macOS
curl "https://awscli.amazonaws.com/AWSCLIV2.pkg" -o "AWSCLIV2.pkg"
sudo installer -pkg AWSCLIV2.pkg -target /
